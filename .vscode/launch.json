{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(ctest) Launch",
      "type": "cppdbg",
      "cwd": "${workspaceFolder}",
      "request": "launch",
      "program": "${cmake.testProgram}",
      "args": [
        "${cmake.testArgs}"
      ],
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "CMake: ビルド"
    }
  ]
}